namespace Neat.StreamManager
{
    public enum TextReaderType
    {
        StreamReader = 1
    }

    public enum TextWriterType
    {
        StreamWriter = 1
    }

    public enum StreamType
    {
        MemoryStream = 1,
        CryptoStream = 2
    }
}